name: workflow

on:
  push:
    branches:
      - '*'

jobs:
  workflow:
    uses: foremanmining/workflows/.github/workflows/java.yml@main
    with:
      result-folder: './'
      gcs-bucket: windows-agent-releases